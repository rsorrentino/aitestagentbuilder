{
  "name": "ai-test-agent-builder-e2e",
  "version": "1.0.0",
  "description": "End-to-end Playwright tests for the AI Test Agent Builder platform UI",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.10.5"
  }
}
